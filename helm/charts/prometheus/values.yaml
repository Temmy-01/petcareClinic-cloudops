replicaCount: 1

image:
  repository: "prom/prometheus"
  tag: "v2.53.0"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 9090

resources:
  limits:
    memory: 256Mi
    cpu: 300m
  requests:
    memory: 128Mi
    cpu: 100m
